# =========================================================
# Date: 2026-04-13 20:20
# Filename: config/boards/shields/Test/IKB_R.conf
# @details
# Zephyr 4.1の仕様（送信バッファより受信バッファを大きくするルール）に対応するため、
# CONFIG_BT_BUF_EVT_RX_COUNT を追加・拡張し、ビルドエラーを解消しました。
# これによりパケット詰まりや遅延を防ぎ、トラックボールを快適に動作させます。
# =========================================================

CONFIG_ZMK_KEYBOARD_NAME="IKB_2"

# --- 基本機能 ---
CONFIG_ZMK_MOUSE=y
CONFIG_NFCT_PINS_AS_GPIOS=y
CONFIG_ZMK_BLE=y
CONFIG_SPI=y
CONFIG_INPUT=y

# --- ZMK Studio ---
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n 

# --- ポインティングデバイス・トラックボール設定 ---
CONFIG_ZMK_POINTING=y
CONFIG_PMW3610_ALT=y
CONFIG_PMW3610_ALT_RUN_DOWNSHIFT_TIME_MS=3264
CONFIG_PMW3610_ALT_REST1_SAMPLE_TIME_MS=20
CONFIG_PMW3610_ALT_SMART_ALGORITHM=y

# --- 通信安定化・バッテリー起因のフリーズ対策 ---
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
CONFIG_ZMK_SLEEP=n
CONFIG_ZMK_BATTERY_REPORTING=n
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=n
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=n

# --- メモリ拡張（全体処理用） ---
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=4096
CONFIG_MAIN_STACK_SIZE=4096
CONFIG_BT_RX_STACK_SIZE=2048

# =========================================================
# 【修正】Bluetoothバッファの拡張（遅延・パケット詰まり・エラー対策）
# =========================================================
CONFIG_BT_L2CAP_TX_BUF_COUNT=10
CONFIG_BT_BUF_ACL_TX_COUNT=10
# Zephyrの仕様により、RXはTX(10)より必ず大きい必要があるため余裕を持たせて16を指定
CONFIG_BT_BUF_EVT_RX_COUNT=16
